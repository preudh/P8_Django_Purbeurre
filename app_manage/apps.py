from django.apps import AppConfig  # Class representing a Django application and its configuration


class AppManageConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_manage'
